---
name: "HTTP request field completion"
document: |
  schemaVersion: scenario/v1
  title: test
  steps:
    - title: step1
      protocol: http
      request:
        $0
operation: completion
expect:
  completionLabels:
    contains: [method, url, body, header]
    excludes: [service, target, metadata]
