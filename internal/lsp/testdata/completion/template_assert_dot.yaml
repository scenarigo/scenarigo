---
name: "template assert dot completion"
document: |
  schemaVersion: scenario/v1
  title: test
  steps:
    - title: step1
      protocol: http
      expect:
        body:
          name: '{{assert.$0'
operation: completion
expect:
  completionLabels:
    contains: [contains, notContains, regexp, notZero, greaterThan, length, and, or, any]
