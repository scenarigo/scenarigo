---
name: "jump to bind.secrets definition"
document: |
  schemaVersion: scenario/v1
  title: test
  steps:
    - title: step1
      protocol: http
      request:
        method: POST
        url: http://example.com
      bind:
        secrets:
          apiKey: "{{response.header.x-api-key}}"
    - title: step2
      protocol: http
      request:
        url: '{{$0secrets.apiKey}}'
operation: definition
expect:
  definitionURI:
    suffix: "test.yaml"
    startLine: 10
    startChar: 8
    endLine: 10
    endChar: 14
