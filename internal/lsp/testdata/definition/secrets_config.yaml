---
name: "jump to config secrets definition in scenarigo.yaml"
document: |
  schemaVersion: scenario/v1
  title: test
  steps:
    - title: step1
      request:
        url: '{{$0secrets.configSecret}}'
operation: definition
files:
  scenarigo.yaml: |
    schemaVersion: config/v1
    secrets:
      configSecret: secret
expect:
  definitionURI:
    suffix: "scenarigo.yaml"
    startLine: 2
    startChar: 2
    endLine: 2
    endChar: 14
